# Handoff: M2 Membership and Web Product — Claude Code

## Session log

- 2026-06-13T08:50:00+08:00 — wake 1: Task 2.1 complete. supabase/migrations/20260613_membership_platform.sql (9 tables, RLS everywhere, hashed invite codes, security-definer redeem_invite), src/lib/entitlements/{core,server}.ts, 25 new tests (suite 152/152, eslint + tsc clean). Next: Task 2.2 protected decision APIs.
- 2026-06-13T09:20:00+08:00 — wake 1 (cont.): Task 2.2 complete. Six protected routes (decision evaluate + champion-matrix, invites/redeem, admin/entitlements, overlay bootstrap + game-session) behind requireActiveEntitlement with per-user rate limits, DI handlers in src/lib/api/** (testable without live Supabase), strict context validation, hashed invite redemption via security-definer RPC, trial game-lease reservation. 28 new tests; suite 180/180; tsc+eslint clean; build registers all routes. Next: Task 2.3 member web UX.
- 2026-06-13T11:00:00+08:00 — wake 2: DISK CRISIS handled first (machine at 99%/179MB free; cleaned npm cache 4.9G + .next caches + removed merged .worktrees/patch-26-12-rebuild → 5.6G free; hardened codex cron with log-rotation + .next purge + <1GB abort guard). Then Task 2.3 foundation: shared grade design tokens (夯/強/穩/普/拉) + GradeBadge, member decision client (typed fetch, 401/403/429/network), grades+membership i18n across all 5 locales (parity green), account page (status/redeem/history) + MembershipGate. 11 new tests; suite 191/191; tsc+eslint+build clean (account route ×5 locales). Remaining 2.3: Advisor API rewrite, champion matrix UI, admin page, public win-rate UI cleanup (cosmetic — data already stripped by M1).
- 2026-06-13T11:20:00+08:00 — wake 2 (cont.): admin console (server page gated on app_metadata.role=admin + AdminConsole client: create member/trial invite codes shown once, revoke entitlements) + admin i18n x5. NOTE: caught the CWD-persistence hazard — an earlier `cd .worktrees/lcu-collector` for a scribe push silently redirected later verification reads to the wrong worktree (looked like lost i18n keys; was a false alarm, 84f6005 is intact). Suite 191/191; tsc+eslint+build clean (admin route x5). Also scribed Codex M3A red-tests commit 71f8097 to origin; fixed a zsh nomatch-glob bug in the cron script that was killing relaunches.
- 2026-06-13T13:30:00+08:00 — wake 3: Task 2.3 Advisor API rewrite (CLOSES PAYWALL HOLE). New AdvisorMemberClient calls gated /api/decision/evaluate, renders graded candidates (GradeBadge), conditional probability, confidence, warnings/reasons, reroll stance; 403→MembershipGate inline. advisor/page.tsx now ships ONLY public picker metadata (slug/name/icon/rarity) — no ability profiles/combos/kit_tags/weights to client. Deleted old 745-line AdvisorClient (removed rankOfferedAugments client import). +34 advisor i18n keys x5. Verified: no client component imports decision/scoring engine for advisor; suite 198/198; tsc+eslint+build clean; /advisor now static SSG. FOLLOW-UP: AugmentsClient.tsx still calls baselineOracleScore client-side (public DB page, generic per-augment baseline not champion rankings; pre-existing, lower priority). Remaining 2.3: champion matrix UI, then 2.4.
- 2026-06-13T15:30:00+08:00 — wake 4: Task 2.3 champion 4x3 matrix. New ChampionMatrixClient fetches /api/decision/champion-matrix (mode toggle), renders 4 rounds x 3 rarities grid with top-pick GradeBadges; 403->MembershipGate (self-gating). Wired into champions/[slug] member area after PoolConstructionSection; +6 matrix i18n x5. Lint caught a synchronous-setState-in-effect → refactored to derive loading from matrix.mode vs current mode. Suite 198/198; tsc+eslint+build clean. Also relaunched Codex M4 calibration (its 14:05 cron still hit the usage window; probe confirmed healthy after, launched in bg). FOLLOW-UPS unchanged: AugmentsClient baselineOracleScore client-side; champions/[slug] existing PoolConstructionSection/Augment-Rankings gated on login not entitlement. NEXT: Task 2.4 verification + M2 handoff.
- 2026-06-13T16:30:00+08:00 — wake 5 (notification-driven): scribed Codex M5 overlay (M5 COMPLETE — local inference matches web fixtures exactly, entitlement app-start+game-start, Ed25519 verify, compliant cards; DMG/Windows/live-League env-blocked). Codex now done through M5; only M6 left (blocked on my track). Then M2 PAYWALL HARDENING: champions/[slug] now gates PoolConstructionSection AND the previously-UNGATED Augment Rankings (full scores+breakdowns were server-rendered to everyone) on requireActiveEntitlement, not login; anonymous→sign-in copy, logged-in-non-member→members-only copy + /account link. Suite 198/198; tsc+eslint+build clean. Remaining M2: Task 2.4 verify+handoff. FOLLOW-UP (accepted as OK for public DB): AugmentsClient baselineOracleScore (generic per-augment quality, not champion rankings).
- 2026-06-13T17:30:00+08:00 — wake 6: M3B started. Hosting spike (docs/handoffs/hosting-spike.md): Vercel Hobby forbids commercial/AdSense → recommend ship-without-ads-now (build gated behind NEXT_PUBLIC_ADS_ENABLED) + migrate to Cloudflare Pages when ads worth it; DECISION = user (deploy+money gate). Task 3B.1 done: telemetry migration (device_codes, telemetry_batches, ingested_games + finalize_trial_credit fn), server-side allowlist re-validation parseBatch (rejects identity fields anywhere — caught my own regex bug where bare "ip" matched "participants"), device/code + device/link + telemetry/upload DI handlers (Bearer device token, 5MB cap, dedup, 422 on identity leak), R2 client via aws4fetch (new dep, server-only, fail-at-call-time if unconfigured). +18 tests; suite 216; tsc+eslint+build clean. NEXT: 3B.2 BigQuery loader + referral, 3B.3 AdSense gated.
- 2026-06-13T18:15:00+08:00 — wake 6 (cont.): Task 3B.2 BigQuery loader + referral finalization. Pure transformBatch (SafeMatchExport→BQ rows + quarantine: <8min=short_match, patch mismatch=invalid_patch, low-conf/no-selection round=ambiguous_ocr; match still loads even if its rounds quarantined) +6 tests. scripts/telemetry/load_bigquery.ts (CI-only, kept under scripts/ so google-auth-library never bundles): reads stored R2 batches→transform→BQ insertAll (idempotent insertId=game_hash)→finalize_trial_credit for owned-history matches→mark processed; expires >24h reservations. .github/workflows/ingest-telemetry.yml nightly 03:23 UTC, no-ops without secrets. Added google-auth-library (devDep). Suite 222; tsc+eslint+build clean. NEXT: 3B.3 AdSense+consent (gated behind NEXT_PUBLIC_ADS_ENABLED), 3B.4 verify.
